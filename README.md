# Opinawaz Universal Public School — Official Website (opnawaz-neobrutalweb)

A high-performance, opinionated base React application built with **Vite** and styled with a modern **Neo-Brutalist** design aesthetic.

---

## ⚡ Quick Start

### 1. Install dependencies
```bash
npm install
```

### 2. Start the development server
```bash
npm run dev
```

The application will run locally at `http://localhost:3000`.

### 3. Build for production
```bash
npm run build
```

---

## 🚀 What's Included

- **React 18**: Modern stateful components with hooks (`useState`, `useEffect`).
- **Vite 5**: Blazing fast instant server start and Hot Module Replacement (HMR).
- **Neo-Brutalist Design Tokens**:
  - High-contrast 3px-4px borders and hard drop shadows (`box-shadow: 4px 4px 0px #121212`)
  - Dynamic multi-palette mood switcher (Sunburst Yellow, Neon Pink, Cyber Cyan, Mint Green, Electric Purple)
  - Dark Mode & Light Mode support via CSS custom properties
  - Neo-brutalist cards, buttons, badges, marquee ticker, and toast notification system
- **Google Fonts**: Space Grotesk, Plus Jakarta Sans, Space Mono.
- **Zero Third-Party CSS Framework Overhead**: Pure vanilla CSS tokens for maximum flexibility and instant performance.

---

## 📁 Project Structure

```
opnawaz neobrutalweb/
├── public/
│   └── favicon.svg         # Custom Neo-Brutalist SVG icon
├── src/
│   ├── App.jsx             # Main interactive application component
│   ├── App.css             # Component layout and sandbox styling
│   ├── index.css           # Neo-Brutalist design tokens & utilities
│   └── main.jsx            # React root mount
├── index.html              # HTML shell & font imports
├── vite.config.js          # Vite configuration
├── package.json            # Scripts & dependencies
└── .gitignore              # Git ignore rules
```
